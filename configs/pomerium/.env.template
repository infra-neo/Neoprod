# Environment Variables Template for Pomerium
# Copy this to .env and fill in the values

# Zitadel OIDC Configuration
# Get these from your Zitadel application configuration at https://gate.kappa4.com
ZITADEL_CLIENT_ID=your-zitadel-client-id-here
ZITADEL_CLIENT_SECRET=your-zitadel-client-secret-here

# Pomerium Secrets
# Generate these with: head -c32 /dev/urandom | base64

# Signing key for JWT tokens
POMERIUM_SIGNING_KEY=generate-with-head-c32-dev-urandom-base64

# Cookie secret for encryption
POMERIUM_COOKIE_SECRET=generate-with-head-c32-dev-urandom-base64

# Shared secret for service-to-service communication
POMERIUM_SHARED_SECRET=generate-with-head-c32-dev-urandom-base64

# Optional: Add custom domain configurations
# POMERIUM_CUSTOM_DOMAIN=pomerium.local
